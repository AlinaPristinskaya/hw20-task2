import React from 'react';
//import { useContext } from 'react';
//import { PriceContext } from './PriceContext';  // Assuming you use context to pass state

function TotalPrice() {
  //const { totalPrice } = useContext(PriceContext);
  console.log(18);
  return (
    //totalPrice > 0 && <h2>Total Price: ${totalPrice}</h2>
    <p>єто я типо посчитал</p>
  );
}

export default TotalPrice;
